<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pib.app.dao.QnaDAO">

    <insert id="create">
		INSERT INTO qnaboard( title
							, content
							, writer )
					 VALUES ( #{title}
					 		, #{content}
					 		, #{writer} )
	</insert>

    <select id="selectBoard" resultType="com.pib.app.vo.Qna">
		SELECT	bno,
				title,
				content,
				writer,
				date
		FROM	qnaboard
		WHERE	1 = 1
		<if test="bno != null">
			AND    bno = #{bno}
		</if>
	</select>
</mapper>
